import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "FomoWidget — Social Proof & Live Activity Notifications",
  description:
    "Boost sales and trust with real-time social proof notifications, live active user counters, and FOMO triggers. Trusted by 3,100+ brands worldwide.",
  keywords: "social proof, fomo, live visitor counter, sales notifications, trust widgets",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col font-sans">{children}</body>
    </html>
  );
}
